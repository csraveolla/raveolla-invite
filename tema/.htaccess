RewriteEngine On

# Skip actual files and directories
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Rewrite slug ke PHP router
RewriteRule ^(.*)$ index.php?slug=$1 [QSA,L]
